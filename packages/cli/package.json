{
  "name": "@weave/cli",
  "version": "0.2.0",
  "description": "Weave CLI — esbuild plugin, `weave build`, `weave dev` (watch + live-reload), and `weave check`.",
  "type": "module",
  "bin": {
    "weave": "./bin/weave.mjs"
  },
  "main": "./src/cli.ts",
  "files": ["src", "bin"],
  "dependencies": {
    "@weave/check": "workspace:*",
    "@weave/compiler": "workspace:*"
  },
  "peerDependencies": {
    "typescript": "^5.7.0",
    "esbuild": "^0.25.0",
    "sass": "^1.0.0"
  },
  "peerDependenciesMeta": {
    "sass": {
      "optional": true
    }
  },
  "license": "MIT"
}
