# Dependencies
node_modules/

# Build output
dist/
build/
# …but NOT the Nx plugin's `build` EXECUTOR source — the unanchored `build/` above would swallow
# `packages/nx/src/executors/build/`, so it was never committed and the published package shipped
# without its `build` executor (`nx build` → "Unable to resolve @weave-framework/nx:build").
!packages/nx/src/executors/build/
!packages/nx/src/executors/build/**
# Staged, pre-compiled @weave-framework/ui tree (tools/build-ui-components.mjs)
packages/ui/.compiled/
# Transient bundles + fixture apps emitted by the verify smokes (each cleans up after itself; the
# patterns are here so an interrupted run leaves nothing to commit by accident).
# Every smoke test's scratch bundle and fixture app. Listed one pattern at a time below, which is how a
# NEW test's fixture leaks: `tools/.verify-entry-name-app-*` was staged into a release commit before
# anyone noticed the list had not grown with it. One rule for the whole convention instead.
tools/.verify-*
tools/.verify-dev-proxy-bundle.mjs
tools/.verify-dev-compiler-error-bundle.mjs
tools/.verify-dev-compiler-app-*/
tools/.verify-check-plain-*/
tools/.verify-check-interp-*/
tools/.verify-check-child-*/
tools/.verify-cli-help-app-*/
tools/.verify-dev-port-bundle.mjs
tools/.verify-dev-runtime-bundle.mjs
tools/.verify-dev-runtime-app-*/
tools/.verify-base-path-bundle.mjs
tools/.verify-base-path-app-*/
tools/.verify-base-router-bundle.mjs
tools/.verify-base-router-app-*/
tools/.verify-lint-location-bundle.mjs
tools/.verify-lint-location-app-*/
tools/.verify-check-fix-bundle.mjs
tools/.verify-check-fix-app-*/
tools/.verify-grow-setup-bundle.mjs
tools/.verify-grow-setup-app-*/
tools/.verify-impact-bundle.mjs
tools/.verify-impact-app-*/
tools/.verify-devtools-bundle.mjs
tools/.verify-devtools-app-*/
tools/.verify-check-outside-*/
tools/.verify-check-shadowed-*/
tools/.verify-forms-inference-*/
tools/.verify-ui-control-*/
tools/.verify-state-bundle.mjs
tools/.verify-state-app-*/
tools/.verify-merge-bundle.mjs
tools/.verify-merge-runner.mjs
tools/.verify-merge-repo-*/
*.tsbuildinfo
# Generated by `weave routes` (file-based routing)
**/routes.gen.ts
# Generated by docs/tools/gen-content.mjs — the full slug→markdown map (search index only), plus one
# module per page so a page's markdown lands in that page's chunk instead of every page's download.
docs/src/content/content.gen.ts
docs/src/content/**/*.gen.ts
# Generated by docs/tools/gen-api.mjs (API reference data)
docs/src/content/api.gen.ts
*.vsix
# ...except the published, downloadable plugin artifacts (this dir ships to public)
!plugins/editor/**/*.vsix

# Test / tooling artifacts
test-results/
playwright-report/
coverage/

# Logs
*.log
npm-debug.log*

# Editor / OS
.vscode/
.DS_Store
Thumbs.db
.idea/
*.swp

# transient esbuild bundle from the cli styles-assets smoke
packages/cli/test/.smoke-*/
packages/cli/test/.smoke-app-*/

# `weave migrate` output (written into the target app; only fixture runs could land it here)
.weave-migrate/
packages/cli/test/fixtures/**/migration-plan.md

# Tarballs written by tools/link-local.mjs — the published shape of this checkout, for an app to install.
.packs/
