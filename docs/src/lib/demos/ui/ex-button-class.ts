import Button from '@weave-framework/ui/button';

// Capitalized tags in the template resolve to this import.
void Button;

/**
 * `class` is forwarded onto the host `<button>`, so layout stays yours — here a utility class
 * stretches the button to fill its container. The Weave classes stay; yours are appended.
 */
export function setup(): Record<string, never> {
  return {};
}
