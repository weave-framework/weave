import Badge from '@weave-framework/ui/badge';

// Capitalized tags in the template resolve to this import.
void Badge;

/**
 * `class` is forwarded onto the Badge container, so layout and theming stay yours. The Weave classes
 * stay; yours are appended — here a utility class tints a `tag` badge.
 */
export function setup(): Record<string, never> {
  return {};
}
