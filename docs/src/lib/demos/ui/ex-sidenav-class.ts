import Sidenav from '@weave-framework/ui/sidenav';

// Capitalized tags in the template resolve to this import.
void Sidenav;

/** `class` is forwarded onto the root element, so you can hook your own styles onto the shell. */
export function setup(): Record<string, never> {
  return {};
}
