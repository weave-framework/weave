import ProgressBar from '@weave-framework/ui/progress-bar';

// Capitalized tags in the template resolve to this import.
void ProgressBar;

/** Indeterminate — a sliding segment for unknown-length work; `aria-valuenow` is omitted. */
export function setup(): Record<string, never> {
  return {};
}
