import Icon from '@weave-framework/ui/icon';

// Capitalized tags in the template resolve to this import.
void Icon;

/** Icons from the built-in Lucide set, by name. */
export function setup(): Record<string, never> {
  return {};
}
