import Input from '@weave-framework/ui/input';

// Capitalized tags in the template resolve to this import.
void Input;

/** disabled / readonly / required — the three native states, forwarded to the field. */
export function setup(): Record<string, never> {
  return {};
}
