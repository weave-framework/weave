import SlideToggle from '@weave-framework/ui/slide-toggle';

// Capitalized tags in the template resolve to this import.
void SlideToggle;

/** disabled / required — the native states, forwarded to the underlying checkbox input. */
export function setup(): Record<string, never> {
  return {};
}
