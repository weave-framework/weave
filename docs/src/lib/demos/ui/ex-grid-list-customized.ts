import GridList from '@weave-framework/ui/grid-list';

// Capitalized tags in the template resolve to this import.
void GridList;

/** Retune the grid via its CSS custom properties: tile size, gap, and corner radius. */
export function setup(): Record<string, never> {
  return {};
}
