import Badge from '@weave-framework/ui/badge';
import Icon from '@weave-framework/ui/icon';

// Capitalized tags in the template resolve to these imports.
void Badge;
void Icon;

/**
 * `label` overrides the host's accessible name. By default a `count` badge announces the bare number;
 * `label` lets a screen reader say something richer like "3 unread notifications".
 */
export function setup(): Record<string, never> {
  return {};
}
